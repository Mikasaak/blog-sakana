Based on the structure of `paul.ren`, I will expand the blog functionality to include a rich homepage with multiple sections (Hero, Blog, Projects, Photography).

### 1. 📂 Content Structure Reorganization
We need to organize content into collections to separate Blog posts from Pages and Projects.
- **Create `content/blog/`**: Move articles here. I will create a few sample posts.
- **Create `content/projects/`**: Store project showcases here.
- **Update `content.config.ts`**: Define `blog` and `projects` collections.

### 2. 🏠 Homepage Implementation (`app/pages/index.vue`)
I will create a dedicated `index.vue` page (which takes precedence over `[...slug].vue` for the root path) to implement the multi-section layout:
- **Hero Section**: A welcoming introduction with a tagline.
- **Latest Articles**: A grid showing the 3 most recent posts from the `blog` collection.
- **Featured Projects**: A horizontal scroll or grid of selected projects.
- **Moments / Photography**: A visual section for photos (using placeholders).

### 3. 🧩 New Components
To keep the code clean, I'll create reusable components:
- `app/components/PostCard.vue`: To display a single blog post preview.
- `app/components/ProjectCard.vue`: To display project details.
- `app/components/SectionTitle.vue`: Uniform section headers.

### 4. 🎨 Styling & Polish
- Use Tailwind CSS to match the clean, spacious aesthetic of the reference site.
- Add hover effects and transitions.

### Execution Steps
1.  **Reorganize Content**: Create directories and sample Markdown files.
2.  **Configure Collections**: Update `content.config.ts`.
3.  **Create Components**: Build the UI cards.
4.  **Build Homepage**: Assemble the sections in `index.vue`.
